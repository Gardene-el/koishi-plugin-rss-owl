/**
 * 联网搜索依赖真实密钥与外部服务，不适合纳入 Jest 自动化测试。
 * 如需联调，请在本地临时编写手工验证代码，并使用环境变量注入凭据。
 */

import { describe, it } from '@jest/globals'

describe.skip('联网搜索手工验证', () => {
  it('需要真实配置后手工执行，不参与自动化测试', () => {
    // 保留占位测试，避免空 suite 导致 Jest 失败。
  })
})